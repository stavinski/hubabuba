/* jshint expr: true */
"use strict";

var expect = require("chai").expect
  , sinon = require("sinon")
  , Hubabuba = require("../");

describe("when handling a notification", function () {

  it("should raise error if id not supplied");
  it("should raise error if link header not supplied");
  it("should respond with a 200");
  it("should raise the notification event");
  it("should make returned notification is a readable stream");
  it("should allow data to be read from notification");
  
});
